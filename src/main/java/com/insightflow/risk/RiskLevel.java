package com.insightflow.risk;

/**
 * 面向运营待办的风险分级；数值越小表示越需要优先响应，
 * 但实际排序始终使用同一时刻冻结的总分，避免仅靠枚举丢失解释依据。
 */
public enum RiskLevel {
    /** 需立即进入风险提醒的高风险异常。 */
    P0,
    /** 需在当前工作周期优先处理的异常。 */
    P1,
    /** 需要跟踪但不应抢占高风险事项的异常。 */
    P2,
    /** 保留在队列中观察的低风险异常。 */
    P3
}
