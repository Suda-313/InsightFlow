# InsightFlow 代码协作规范

## 核心工程原则

- 遵循 KISS 和 YAGNI：优先使用能解决当前需求的最少代码，不为未被确认的场景预先增加功能、抽象、配置项或兼容层。
- 对用户提出的方案先独立判断再执行；若存在更简单、更安全或更符合项目边界的做法，应说明理由并给出建议，而不是为了迎合而直接同意。
- 遇到需求含义不唯一、关键约束缺失或方案风险无法判断时，应明确说明不确定点并请求确认；不要静默选择会改变产品方向的假设。
- 默认采用外科手术式改动：只修改与需求直接相关的代码，遵循现有风格，不顺手重构、格式化或删除无关代码。发现无关问题时先报告，等待用户确认。
- 仅当用户明确要求“最佳实践重构”或“重写”时，才可以在确认目标后进行干净重构；不要为旧接口堆叠无价值的兼容层。
- 对跨文件、涉及数据模型或存在多种方案的改动，先说明假设、权衡与推荐方案并取得确认；一行机械性改动可直接完成。

## 沟通与交付

- 最终回复必须使用中文，先给出结论、发现和影响，再说明实现或验证细节。
- 引用代码位置时使用 `路径:行号`；讨论问题时针对代码和设计，不针对个人。
- 将需求转换为可验证目标：修复 Bug 先写复现测试，新增校验先覆盖非法输入，重构前后均应保持相关测试通过。
- 未运行与改动范围相匹配的验证命令前，不得声称“已完成”“已修复”或“测试通过”。交付前检查可维护性、测试、性能、安全性和兼容性。
- 未经用户明确要求，不执行提交、推送、重置、删除或其他不可逆 Git 操作。

## 文档规范

日常维护以**两个主文档**为准；其余目录视为历史档案或业务素材，不重复写同一事实。

| 文档 | 用途 | 何时更新 |
|------|------|----------|
| `docs/project-development-log.md` | **已验证事实**与面试/复盘素材 | 完成有含金量的能力、跨层修复或架构取舍，且已通过测试或运行验证 |
| `docs/agent-optimization-todo.md` | **待办与方向** | 识别到值得做、尚未交付的优化；完成后勾选，事实摘要补进 development-log |
| `docs/HANDOFF.md` | 换机/换会话交接 | 阶段性使用；结论合并进 development-log 后应缩短，不长期堆叠 |
| `docs/superpowers/specs\|plans/` | 功能设计稿与实施计划 | **归档只读**；新功能不再默认新建 plan，除非用户明确要求完整设计流程 |
| `docs/knowledge-sources/` | 知识库样例与业务素材 | 与工程记录分离，不计入开发日志 |

面试用 STAR、一页纸提纲等**个人复盘笔记**放在仓库外（Notion / 本地笔记均可），不要把同一案例在多个 md 里各写一遍。

### 什么该写入 development-log

满足至少一条：**跨层**（前端 + 后端 + 数据/任务）、**有设计取舍**、**可复述根因**、**有验证命令或指标**。

- 跨层 Bug 的根因与护栏（例如：异步任务 + HTTP 分层超时、API 契约与前端状态不一致）
- 架构或产品边界调整（例如：Agent 只读、复核队列准入、Workspace 隔离）
- 异步任务、评测体系、RAG/Agent 行为变更
- 数据模型、迁移、API 契约的不可逆决策
- 可量化的可靠性/性能改进（附验证方式，不编造数字）

每条固定六段，只写**已验证**内容：

```md
### 日期：问题标题

- **背景或现象：**
- **根因：**
- **方案与取舍：**
- **实现：**
- **验证：**
- **沉淀：**
```

### 什么不必写入 development-log

- Token/JWT 过期、本地 `application-local.yml` 漏配、端口占用、依赖未安装
- 单次 typo、改一行配置、纯 UI 文案、无业务含义的格式化
- 尚未实现的想法（写入 todo，标 `[ ]`）
- 未跑过相关测试或人工验证的「预期效果」

### todo 与 development-log 的分工

- **todo**：`[ ]` 待办、`[x]` 已完成；可含验收标准与背景一句，不写长文。
- **development-log**：todo 项**交付并验证后**，用一条六段记录摘要；不要在 todo 里复制整段 log。

### Agent 执行约定

- 完成**有含金量**的改动后，在同一轮交付中更新 development-log（或说明为何本次跳过）。
- 不要为每个 Bug 新建独立 md；不要顺手更新 `HANDOFF.md` 或 `superpowers/` 除非用户明确要求。
- 文档只陈述可核验事实；简历/对外表述中的指标须与 log 或测试结果一致，不夸大。

## 安全与配置

- 禁止将 API Key、密码、Token、个人信息或未脱敏业务数据写入源码、默认配置、测试夹具或日志；密钥只能通过环境变量或受控部署配置读取。
- 不保存或展示模型原始思维链；需要解释时输出可核验的分析依据、数据来源和不确定项。
- 发现已存在的无关死代码、敏感配置或安全风险时，应先报告影响和处理建议，除非用户明确授权，不扩大本次改动范围。

## 注释要求

- 对业务模块、数据库实体与迁移、API 契约、异步任务、Agent / Skill / Harness 护栏等关键代码，**有效注释行数不得少于非空代码行数的 1/2**；
- 注释必须解释业务目的、约束、边界条件或设计原因，禁止用“获取名称”“设置字段”等机械复述凑数量；
- 数据库实体须说明内部主键与对外 `public_id` 的职责、Workspace 隔离要求和关键字段的业务含义；
- 迁移文件须说明不可逆的结构决策、索引目的和与 API / 领域模型的对应关系；
- 每次新增关键模块时，在同一变更内补齐注释并运行相应测试；不允许实现完成后再集中补注释。

## 架构边界

- 保持模块化单体；不引入多 Agent、微服务、真实爬虫或未确认的数据源；
- 所有业务读写必须按 `workspace_id` 隔离；外部 API 只暴露 `public_id`；
- Agent 只读调查，策略变更必须经过人工确认；不得绕过 Tool / Guardrail / Trace。
